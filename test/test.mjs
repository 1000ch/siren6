import assert from 'assert';

describe('草', function () {
    describe('hogehoge', function () {
        it('hogehoge', function () {
            assert.equal(1, 2);
        });
    });
});
